syntax = "proto3";

package user;

option go_package = "user/";

service UsersProviderService {
    rpc GetAllUsers(GetAll) returns (Users);
}

message GetAll {}

message Users {
    repeated UserData user = 1;
}

message UserData {
    string id = 1;
    string name = 2;
}
